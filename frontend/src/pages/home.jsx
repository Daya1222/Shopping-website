import { useState } from "react";
import { useUser } from "../hooks/useUser";
import 

function Home() {
  const [user, setuser] = useUser();

  return <div>

    <span>
ssh
       
    </span>

  </div>;
}

export default Home;
